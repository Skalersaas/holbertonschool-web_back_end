#!/usr/bin/env python3
''' Description: Type-annotated function to_str
    that takes a float n as argument and
    returns the string representation of the float.
    Arguments: n: float
'''


def to_str(n: float) -> str:
    '''Returns sum of two numbers with floating point'''
    return str(n)
